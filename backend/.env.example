# This is an example file. For local development, you can copy this to a .env file.
# In production, these variables should be set in the deployment environment (e.g., AWS Secrets Manager or ECS Task Definition).

# Django Settings
SECRET_KEY=your-super-secret-and-long-key-here
DEBUG=True

# Database Settings (for Docker Compose)
POSTGRES_DB=intelligent_insure
POSTGRES_USER=postgres
POSTGRES_PASSWORD=Ayman1@1
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Frontend URL for CORS
CORS_ALLOWED_ORIGINS=http://localhost:3000
