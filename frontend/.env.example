# The public URL of your backend API.
# For local development with Docker, this will point to the backend service.
# In production, this would be your deployed API's URL (e.g., https://api.yourdomain.com).
NEXT_PUBLIC_API_URL=http://localhost:8000
